{
    "nodes": [
        {
            "id": "0",  // 必须有的唯一标识
            "label": "n0",  // 节点下方的标签文字，可以省略
            "type": "user"  // 这个节点的类型，例如商家节点、普通用户节点等，非必须
        }
    ],
    "edges": [
        {
            "source": "0",  // 一条边的起点，必须
            "target": "1",  // 一条边的终点，必须
            "label": "e0-1", // 边上面的文字说明， 非必须
            "type": "user"  // 边的类型，同上述节点类型一样，非必须
        }
    ]
}
